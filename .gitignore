.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/

.env*

# Local data + run artifacts (large, regenerated)
data/*.parquet
data/*.csv
data/*.duckdb
data/raw/
runs/

# Dagster instance state (SQLite DBs, compute logs, schedules) created by
# `diageo dagster-dev`. Pure local state; the schema-level config lives in
# the committed `dagster.yaml` at the repo root.
.dagster_home/
.local_context/

# Editor
.vscode/
.idea/
.DS_Store
